{
    "$schema": "https://json.schemastore.org/claude-code-settings.json",
    "_comment": "Layer 20 — merged over ~/.claude-layers/00-baseline.json by entrypoint.sh. Declares only what the web image ADDS to the baseline MCP set.",
    "mcpServers": {
        "playwright": {
            "command": "npx",
            "args": [
                "-y", "@playwright/mcp",
                "--browser", "chromium",
                "--executable-path", "/usr/bin/chromium",
                "--no-sandbox"
            ]
        },
        "fetch": {
            "_comment": "uvx, NOT npx. @modelcontextprotocol/server-fetch does not exist on npm (404); the fetch server is published on PyPI as mcp-server-fetch.",
            "command": "uvx",
            "args": ["mcp-server-fetch"]
        },
        "sqlite": {
            "command": "uvx",
            "args": ["mcp-server-sqlite", "--db-path", "/workspace/.dev.db"]
        }
    }
}
